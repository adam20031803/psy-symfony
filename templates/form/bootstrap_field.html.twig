{# Label, champ, puis messages d’erreur (invalid-feedback) — rien avant soumission du formulaire. #}
<div class="mb-3 mental-field">
    {{ form_label(field) }}
    {{ form_widget(field) }}
    {{ form_errors(field) }}
</div>
